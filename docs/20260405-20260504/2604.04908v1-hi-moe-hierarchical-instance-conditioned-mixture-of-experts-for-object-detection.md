---
title: "HI-MoE: Hierarchical Instance-Conditioned Mixture-of-Experts for Object Detection"
title_zh: HI-MoE：用于目标检测的分层实例条件混合专家网络
authors: "Vadim Vashkelis, Natalia Trukhina"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04908v1"
tags: ["query:sod"]
score: 6.0
evidence: 用于实例条件目标检测的DETR风格检测架构
tldr: 针对目标检测中现有MoE方法在图像或补丁级别路由与实例级推理不匹配的问题，本文提出HI-MoE架构。该方法采用分层路由策略，结合场景级和实例级选择，使DETR类检测器的专家分配更符合目标特性。在COCO和LVIS数据集上的实验表明，HI-MoE在保持稀疏计算的同时，显著提升了检测性能，尤其是在小目标检测上表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04908v1/fig-001.webp\", \"caption\": \"Table 2: HI-MoE forward routing procedure. The algorithm first computes scene-level routing from global context, then performs query-wise instance routing within the scene-constrained expert pool.\", \"page\": 6, \"index\": 1, \"width\": 944, \"height\": 1304}, {\"url\": \"assets/figures/arxiv/2604.04908v1/fig-002.webp\", \"caption\": \"Figure 2: Visualization derived from the expert-level routing statistics in Table 8. Left: per-expert subset AP for representative experts and the average row. Right: for each displayed expert, the proportion of that expert’s routed assignments associated with its dominant scene-route category (Crowd for E1, Indoor for E3, Outdoor for E6). These percentages are normalized independently per expert and therefore are not additive and are not expected to sum to 100% across the three bars. This figure is intended as a first-step illustration of specialization rather than a complete utilization analysis across all experts and layers.\", \"page\": 10, \"index\": 2, \"width\": 940, \"height\": 516}, {\"url\": \"assets/figures/arxiv/2604.04908v1/fig-003.webp\", \"caption\": \"Figure 1: HI-MoE overview. A scene router first selects a scene-consistent expert subset; an instance router then performs per-query top-K routing inside that subset. Sparse experts replace selected transformer FFNs.\", \"page\": 4, \"index\": 3, \"width\": 727, \"height\": 558}, {\"url\": \"assets/figures/arxiv/2604.04908v1/fig-004.webp\", \"caption\": \"Table 1: Notation summary for the hierarchical routing module.\", \"page\": 4, \"index\": 4, \"width\": 468, \"height\": 402}]"
motivation: 现有的视觉MoE多基于图像或补丁级别，与目标检测中以实例为核心的推理单元不匹配。
method: 提出分层实例条件MoE，通过轻量级场景路由器筛选专家子集，再由实例路由器将每个查询分配给该子集中的特定专家。
result: 在COCO数据集上超越了DINO基线及其他路由变体，并在小目标检测和专家专业化分析中表现出明显优势。
conclusion: HI-MoE通过分层路由机制有效地将MoE应用于目标检测，证明了实例级稀疏计算在提升检测精度方面的潜力。
---

## 摘要
混合专家（MoE）架构通过为每个输入仅激活模型参数的一个子集来实现条件计算。尽管稀疏路由在语言模型中非常有效，并且在视觉领域也展现出潜力，但大多数视觉 MoE 方法都在图像或补丁（patch）级别运行。这种粒度与目标检测任务的契合度较低，因为目标检测的基本推理单元是对应于候选实例的对象查询（object query）。我们提出了分层实例条件混合专家网络（HI-MoE），这是一种 DETR 风格的检测架构，分两个阶段执行路由：轻量级场景路由器首先选择一个场景一致的专家子集，然后实例路由器将每个对象查询分配给该子集中的少量专家。该设计旨在保持稀疏计算的同时，更好地匹配检测任务中异构的、以实例为中心的结构。在当前的草案中，实验集中在 COCO 数据集上，并在 LVIS 上进行了初步的专业化分析。在这些设置下，HI-MoE 优于稠密 DINO 基线以及更简单的令牌级或仅实例路由变体，尤其在小目标上提升显著。我们还提供了专家专业化模式的初步可视化。我们以旨在支持进一步实验验证的形式展示了该方法、消融实验和当前的局限性。

## Abstract
Mixture-of-Experts (MoE) architectures enable conditional computation by activating only a subset of model parameters for each input. Although sparse routing has been highly effective in language models and has also shown promise in vision, most vision MoE methods operate at the image or patch level. This granularity is poorly aligned with object detection, where the fundamental unit of reasoning is an object query corresponding to a candidate instance. We propose Hierarchical Instance-Conditioned Mixture-of-Experts (HI-MoE), a DETR-style detection architecture that performs routing in two stages: a lightweight scene router first selects a scene-consistent expert subset, and an instance router then assigns each object query to a small number of experts within that subset. This design aims to preserve sparse computation while better matching the heterogeneous, instance-centric structure of detection. In the current draft, experiments are concentrated on COCO with preliminary specialization analysis on LVIS. Under these settings, HI-MoE improves over a dense DINO baseline and over simpler token-level or instance-only routing variants, with especially strong gains on small objects. We also provide an initial visualization of expert specialization patterns. We present the method, ablations, and current limitations in a form intended to support further experimental validation.