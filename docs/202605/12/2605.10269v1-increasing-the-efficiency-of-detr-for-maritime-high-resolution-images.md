---
title: Increasing the Efficiency of DETR for Maritime High-Resolution Images
title_zh: 提高DETR在海事高分辨率图像中的效率
authors: "Tinsae Yehuala, Hao Cheng, Ville Lehtola"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10269v1"
tags: ["query:sod"]
score: 9.0
evidence: 使用Vision Mamba准确识别从小浮标到大型船只的障碍物
tldr: 针对无人船在处理高分辨率海面图像时面临的计算资源受限和检测精度挑战，本文提出了一种基于Vision Mamba（ViM）的高效DETR改进方案。通过引入状态空间模型（SSM）实现线性序列缩放，并结合定制的特征金字塔网络与令牌剪枝技术，在保持长距离依赖建模能力的同时显著降低了计算开销，实现了性能与效率的更佳平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10269v1/fig-001.webp\", \"caption\": \"Fig. 1: To achieve more efficient maritime object detection with high-resolution images compared to the current state-ofthe-art RT-DETR [6] based detector (a), our method leverages ViM [10] with state space models (SSMs) [11], [12] and a tailored Efficient FPN to emphasize the foreground containing target objects by pruning the background tokens with an MLP classifier (b). By incorporating State Spacebased vision models, such as VSSD-50 [13] and ViM-50 [6], our approach achieves more efficient GPU usage as the input image size increases (c).\", \"page\": 1, \"index\": 1, \"width\": 512, \"height\": 625}, {\"url\": \"assets/figures/arxiv/2605.10269v1/fig-002.webp\", \"caption\": \"Fig. 5: Visualization of maritime object detection. Left: Ground truth. Right: Prediction with the proposed method using a 32× 32 patch size.\", \"page\": 7, \"index\": 2, \"width\": 1052, \"height\": 1193}, {\"url\": \"assets/figures/arxiv/2605.10269v1/fig-003.webp\", \"caption\": \"Fig. 2: Background-Aware Linear-Scaling Backbone. The flattened image is first passed through a shallow pre-backbone composed of SSM blocks. A shallow MLP then classifies the patches into foreground and background tokens. The foreground tokens are forwarded to the main backbone, also built with SSM blocks, which is responsible for extracting the feature map. To avoid extra computation, the background tokens are not processed further but retain their pre-backbone representations.\", \"page\": 3, \"index\": 3, \"width\": 1052, \"height\": 218}]"
motivation: 传统目标检测模型在处理高分辨率海面图像时存在内存需求高、小目标识别难以及边缘计算资源受限的问题。
method: 采用Vision Mamba作为骨干网络，并设计了包含连续下采样与SSM层的特征金字塔网络，同时引入令牌剪枝以减少背景区域的冗余计算。
result: 与RT-DETR等现有先进模型相比，该方法在海面目标检测任务中实现了更高的计算效率和更优的性能平衡。
conclusion: 结合SSM与令牌剪枝的DETR架构能有效提升高分辨率海面图像检测的效率，适用于资源受限的无人船导航场景。
---

## 摘要
海上目标检测对于无人船（USV）的安全航行至关重要，需要准确识别从小型浮标到大型船只的各种障碍物。由于距离远、目标尺寸小、尺度变化大、边缘计算限制以及高分辨率图像的高内存需求，实时检测面临巨大挑战。现有的解决方案（如降采样或图像分割）往往会降低精度或需要额外的处理，而内存高效型模型通常只能处理有限的分辨率。为了克服这些局限性，我们利用了 Vision Mamba (ViM) 骨干网络，该网络基于状态空间模型 (SSM) 构建，在捕获远程依赖关系的同时，其计算量随序列长度线性扩展。图像被标记化为序列，以实现高效的高分辨率处理。为了进一步提高计算效率，我们设计了一个定制的特征金字塔网络，包含连续的降采样和 SSM 层，并采用令牌剪枝来减少背景区域的不必要计算。与使用 ResNet50 骨干网络的 RT-DETR 等最先进方法相比，我们的方法在海上目标检测中实现了性能与计算效率之间更好的平衡。

## Abstract
Maritime object detection is critical for the safe navigation of unmanned surface vessels (USVs), requiring accurate recognition of obstacles from small buoys to large vessels. Real-time detection is challenging due to long distances, small object sizes, large-scale variations, edge computing limitations, and the high memory demands of high-resolution imagery. Existing solutions, such as downsampling or image splitting, often reduce accuracy or require additional processing, while memory-efficient models typically handle only limited resolutions. To overcome these limitations, we leverage Vision Mamba (ViM) backbones, which build on State Space Models (SSMs) to capture long-range dependencies while scaling linearly with sequence length. Images are tokenized into sequences for efficient high-resolution processing. For further computational efficiency, we design a tailored Feature Pyramid Network with successive downsampling and SSM layers, as well as token pruning to reduce unnecessary computation on background regions. Compared to state-of-the-art methods like RT-DETR with ResNet50 backbone, our approach achieves a better balance between performance and computational efficiency in maritime object detection.

---

## 论文详细总结（自动生成）

这篇论文针对无人船（USV）在海事环境下处理高分辨率图像时面临的计算瓶颈，提出了一种基于 Vision Mamba 的高效目标检测框架。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
海事目标检测（如识别浮标、船只）对无人船的安全航行至关重要。其核心挑战在于：
*   **高分辨率需求：** 为了检测远距离的小目标，通常需要 2M 甚至 10M 像素的图像，但传统 CNN 或 Transformer 模型的计算量随分辨率呈平方级增长。
*   **资源受限：** 无人船通常部署在计算能力和内存有限的边缘设备上。
*   **环境冗余：** 海面图像中大部分区域是天空和水，对这些背景区域进行密集计算造成了极大的资源浪费。
*   **尺度变化：** 目标从微小浮标到巨型货轮，尺度跨度极大。

### 2. 方法论：核心思想与关键技术
论文提出了一种结合状态空间模型（SSM）和令牌剪枝的改进型 DETR 架构：
*   **Vision Mamba (ViM) 骨干网络：** 核心思想是利用 SSM 的线性扩展特性。将图像切分为 Patch 并序列化，通过双向 SSM 捕捉长程依赖，其时间复杂度为 $O(TD)$，内存复杂度为 $O(D)$，远优于 Transformer 的平方级复杂度。
*   **背景令牌剪枝 (Background Pruning)：** 
    *   图像经过浅层 SSM 预处理后，通过一个轻量级 MLP 分类器对 Patch 进行评分。
    *   仅保留得分最高的 $K$ 个前景令牌进入后续深层骨干网络，背景令牌保留初始表示但不参与后续重度计算，最后再拼接回原位置。
*   **高效特征金字塔 (Efficient FPN)：** 
    *   取消了传统 FPN 中耗时的上采样操作。
    *   采用连续的深度卷积（Depthwise Convolution）下采样，并在阶段间插入 SSM 层，利用 SSM 的全局感受野来弥补上采样路径缺失带来的语义损失。
*   **损失函数：** 结合了检测损失（二分图匹配+匈牙利损失）和针对类别不平衡加权的 Patch 分类损失（BCE）。

### 3. 实验设计
*   **数据集：** 使用新加坡海事数据集（Singapore Maritime Dataset, SMD），图像分辨率为 $1920 \times 1080$。
*   **基准模型 (Benchmark)：** 以最先进的实时检测器 **RT-DETR (ResNet50)** 为主要对比对象。
*   **对比变体：** 实现了基于 Vision Transformer (ViT) 和 Vision State Space Duality (VSSD) 的版本，以验证不同架构在相同框架下的表现。
*   **评价指标：** mAP（含小、中、大目标）、GPU 内存占用、GFLOPs、FPS。

### 4. 资源与算力
*   **硬件环境：** 实验特别强调了在边缘设备上的表现，测试使用的是 **4GB NVIDIA Quadro T600 Mobile GPU**。
*   **训练细节：** Batch size 为 6（RT-DETR）或 8（其他模型）；使用 AdamW 优化器；训练 100 个 Epoch。
*   **算力优势：** 相比 RT-DETR，该方法在 1080P 分辨率下显著降低了显存需求。

### 5. 实验数量与充分性
*   **实验组数：** 进行了测试集和验证集的性能对比、计算效率对比（参数/算力/帧率）、以及详细的消融实验。
*   **消融实验：** 涵盖了 FPN 结构（简单 vs 高效）、Patch 尺寸（16x16, 24x24, 32x32）、剪枝比例（0% vs 50%）对精度和算力的影响。
*   **充分性评价：** 实验设计较为客观，直接针对“高分辨率”和“边缘设备”这两个痛点进行了量化对比，证明了 Mamba 架构在长序列处理上的优势。

### 6. 主要结论与发现
*   **效率飞跃：** 在 T600 移动 GPU 上，该方法比 RT-DETR 实现了 **6 倍的推理加速**（从 1.1 FPS 提升至 6.9 FPS），并减少了 **50% 的内存占用**。
*   **精度保持：** 在大幅提升速度的同时，mAP 表现稳健。基于 VSSD 的版本在测试集上达到了 79.0 mAP，超过了 RT-DETR 的 75.3。
*   **剪枝有效性：** ViM 模型在前景分类上表现最准（85.4% 准确率），能够有效识别并跳过海面/天空背景。
*   **线性扩展：** 实验证实了随着分辨率增加，SSM 架构的 GPU 内存增长曲线远平缓于 CNN 和 Transformer。

### 7. 优点
*   **架构创新：** 成功将 Mamba 的线性扩展特性引入海事检测，解决了高分辨率图像处理的成本问题。
*   **针对性强：** 背景剪枝策略极度契合海面场景（高冗余背景）。
*   **工程实用性：** 专门在低端移动 GPU 上测试，对无人船的实际部署具有很高的参考价值。

### 8. 不足与局限
*   **小目标精度波动：** 虽然整体 mAP 较高，但在某些配置下，由于令牌剪枝和 Patch 化处理，极小目标的检测精度相比密集型模型仍有轻微下降风险。
*   **数据集单一：** 仅在新加坡海事数据集（SMD）上进行了验证，未在更多样化的海况（如大雾、高浪、夜间）或其他公开海事数据集上测试泛化性。
*   **剪枝超参数：** 剪枝比例 $r$ 目前是固定的超参数，未能实现根据图像内容动态调整剪枝强度的自适应机制。

（完）
