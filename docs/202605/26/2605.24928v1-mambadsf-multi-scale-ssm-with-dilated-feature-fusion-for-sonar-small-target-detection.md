---
title: "MambaDSF: Multi-Scale SSM with Dilated Feature Fusion for Sonar Small Target Detection"
title_zh: MambaDSF：用于声呐小目标检测的空洞特征融合多尺度状态空间模型
authors: "Hui Lin, Jiayi Li, Jing Wang, Shenghui Rong"
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2605.24928v1"
tags: ["query:sod"]
score: 10.0
evidence: 用于声呐小目标检测的多尺度特征融合和针对小目标的监督学习
tldr: "针对水声图像中小目标检测面临的低对比度、像素稀缺及尺度模糊等挑战，本文提出MambaDSF框架。该方法结合了Mamba增强特征金字塔（MambaEFP）以兼顾局部与全局上下文，利用空腔融合Mamba（DFMamba）实现多尺度特征对齐，并引入尺度自适应加权IoU与跨尺度一致性损失。实验表明，该模型在UATD基准上达到91.5%的mAP50，显著提升了小目标检测性能与泛化能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24928v1/fig-001.webp\", \"caption\": \"Figure 4: Qualitative comparison across three sonar datasets (one row per dataset: UATD, FLS, MD-FLS) and eight methods. Ground-truth boxes in green; TP/FN/FP counts annotated below each sub-image.\", \"page\": 6, \"index\": 1, \"width\": 976, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2605.24928v1/fig-002.webp\", \"caption\": \"Figure 1: Overall architecture of MambaDSF. The MambaEFP backbone extracts multi-scale features at three pyramid levels. The DFMamba encoder refines each level via multi-scale dilated attention and cross-scale SSM fusion. A query-based decoder yields detections from 300 learnable queries.\", \"page\": 2, \"index\": 2, \"width\": 970, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2605.24928v1/fig-003.webp\", \"caption\": \"Figure 2: DFMamba encoder architecture. {N3, N4, N5} from MambaEFP are split into nd dilated attention branches by the MSDA module. Each branch undergoes independent cross-scale fusion in the SCM Block via FusSSM, whose gating parameters derive from spatially aligned auxiliary features of adjacent pyramid levels. The AFR module aggregates all branches and produces {E3, E4, E5} for the decoder.\", \"page\": 3, \"index\": 3, \"width\": 874, \"height\": 216}, {\"url\": \"assets/figures/arxiv/2605.24928v1/fig-004.webp\", \"caption\": \"Figure 3: Detailed module architectures. (a) Hybrid Block: parallel DW3×3 and DW5×5 branches preserve local spatial detail alongside the SSM branch, merged via learnable residual weights. (b) SCM and AFR Blocks: MSDA splits features into nd branches; FusSSM derives SSM parameters (∆,B,C) from aligned auxiliary features and applies multiplicative gating for cross-scale fusion.\", \"page\": 4, \"index\": 4, \"width\": 622, \"height\": 345}, {\"url\": \"assets/figures/arxiv/2605.24928v1/fig-005.webp\", \"caption\": \"Table 1: Comparison across four evaluation benchmarks. All models were pretrained on UATD and fine-tuned on each target dataset under identical settings. Best results in bold, second-best underlined.\", \"page\": 5, \"index\": 5, \"width\": 988, \"height\": 362}]"
motivation: 传统CNN缺乏全局上下文且Transformer计算成本高，现有Mamba模型在处理声呐小目标时存在多尺度对齐不足和训练不稳定的问题。
method: 提出包含MambaEFP骨干网络、DFMamba编码器以及SA-WIoU和CSC损失函数的混合框架，实现高效的特征提取与多尺度融合。
result: "在UATD前视声呐基准上取得91.5%的mAP50，在小目标子集上提升了2.2个百分点，且具有良好的跨域泛化性。"
conclusion: MambaDSF通过线性复杂度的状态空间模型有效解决了声呐小目标检测中的尺度与上下文建模难题，是水下目标检测的有力工具。
---

## 摘要
声呐成像是水下目标检测的主要手段，但由于像素覆盖不足、声学对比度低以及成像范围带来的尺度模糊，小目标检测仍然具有挑战性。基于卷积神经网络（CNN）的检测器能高效提取局部特征，但在缺乏全局声学上下文的情况下无法抑制噪声引起的虚警。基于Transformer的方法以二次计算成本捕获远程依赖。现有的基于Mamba的视觉模型虽然提供了高效的线性成本扫描，但缺乏金字塔层级间的多尺度语义对齐、多感受野融合以及可靠声呐检测所需的小目标感知训练监督。本文提出了Mamba空洞尺度融合（MambaDSF）框架，通过三项贡献解决这些局限性：一是Mamba增强特征金字塔（MambaEFP）主干网络，以线性复杂度同时捕获局部回波线索和全局声学上下文；二是空洞融合Mamba（DFMamba）编码器，强制实现金字塔层级间的多尺度特征对齐；三是尺度自适应加权交并比（SA-WIoU）和跨尺度一致性（CSC）损失函数，用于稳定小目标训练。MambaDSF在UATD前视声呐基准测试中以2870万个参数实现了91.5%的mAP50，超越了所有对比检测器。在小目标子集上，增益达到了+2.2个百分点，且在前视声呐（FLS）和MD-FLS上的跨域评估证实了该架构的泛化能力。代码已开源至 https://github.com/IDontKnowAAA/MambaDSF。

## Abstract
Sonar imaging is the primary modality for underwater target detection, yet small targets remain difficult to detect due to insufficient pixel coverage, low acoustic contrast, and scale ambiguity across imaging ranges. CNN-based detectors extract local features efficiently but cannot suppress noise-induced false alarms without global acoustic context. Transformer-based methods capture long-range dependencies at quadratic computational cost. Existing Mamba-based vision models offer efficient linear-cost scanning but lack multi-scale semantic alignment across pyramid levels, multi-receptive-field fusion, and small-target-aware training supervision needed for reliable sonar detection.   This letter proposes Mamba Dilated-Scale Fusion (MambaDSF), a hybrid framework addressing these limitations through three contributions: a Mamba Enhanced Feature Pyramid (MambaEFP) backbone that jointly captures local echo cues and global acoustic context at linear complexity; a Dilate Fusion Mamba (DFMamba) encoder that enforces multi-scale feature alignment across pyramid levels; and Scale-Adaptive Weighted IoU (SA-WIoU) and Cross-Scale Coherence (CSC) losses that stabilize small-target training. MambaDSF achieves 91.5% mAP50 on the UATD forward-looking sonar benchmark with 28.7 million parameters, surpassing all compared detectors. On a small-target subset the gain reached +2.2 percentage points, and cross-domain evaluation on FLS and MD-FLS confirms the generalization of the proposed architecture. The codes are publicly available at https://github.com/IDontKnowAAA/MambaDSF.

---

## 论文详细总结（自动生成）

### 论文总结：MambaDSF —— 用于声呐小目标检测的空洞特征融合多尺度状态空间模型

#### 1. 核心问题与整体含义（研究动机和背景）
声呐成像是水下探测的核心手段，但声呐图像中的**小目标检测**面临三大挑战：
*   **特征稀疏**：经过多次下采样后，小目标在特征图上可能仅剩一两个像素。
*   **低对比度**：目标回波强度与背景混响、斑点噪声接近，导致高虚警率。
*   **尺度模糊**：目标的表观尺寸随成像距离和掠射角剧烈变化。
传统的 CNN 缺乏全局上下文，Transformer 计算成本过高（$O(N^2)$），而现有的 Mamba（状态空间模型，SSM）虽然具有线性复杂度，但在多尺度语义对齐、多感受野融合以及针对小目标的训练监督方面存在不足。

#### 2. 论文提出的方法论
MambaDSF 采用了一种混合架构，核心由三个部分组成：
*   **MambaEFP 骨干网络**：
    *   集成 **MambaVision** 提取分层特征。
    *   引入 **Hybrid Block**：通过并行的深度卷积（DW Conv）分支保留局部回波细节，弥补 Mamba 将 2D 图像展平为 1D 序列时造成的空间信息损失。
    *   **增强型双向特征金字塔（E-FPN）**：包含对比度增强、边缘注意力及多尺度增强模块，专门强化声呐目标的强度和边界特征。
*   **DFMamba 编码器（Dilate Fusion Mamba）**：
    *   **多尺度空洞注意力（MSDA）**：通过不同空洞率的分支捕获不同范围的感受野。
    *   **选择性跨尺度调制（SCM）**：利用相邻层级的特征作为调制器，通过 **FusSSM** 动态调整当前层级的门控参数，实现跨尺度的语义对齐。
*   **任务特定损失函数**：
    *   **SA-WIoU（尺度自适应加权 IoU）**：结合了归一化 Wasserstein 距离（NWD）和 CIoU。对于不重叠的小目标，NWD 能提供非零梯度，解决回归梯度消失问题。
    *   **CSC（跨尺度一致性损失）**：通过余弦相似度约束不同特征层级对同一目标的表示，消除多尺度间的语义冲突。

#### 3. 实验设计
*   **数据集**：
    *   **UATD**：包含 9,200 张多波束前视声呐图像，涵盖 10 个类别。
    *   **UATD 小目标子集**：筛选面积最小的 25% 标注目标进行专项评估。
    *   **FLS & MD-FLS**：用于跨域泛化性测试（在 UATD 预训练后进行微调）。
*   **对比方法**：
    *   **CNN 类**：Faster R-CNN, YOLOv3, YOLOv5-L, YOLOv8-L, YOLOv10-L。
    *   **Transformer 类**：Deformable DETR, DINO-DETR, RT-DETR R50, LS-DETR（声呐专用）。
*   **评价指标**：mAP50, mAP50-95, 参数量 (Params), 计算量 (GFLOPs), 帧率 (FPS)。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **软件/参数**：输入分辨率 640×640，使用 AdamW 优化器。
*   **训练细节**：跨域评估时，在主干网络冻结的情况下微调 50 个 epoch。文中未明确给出总训练时长，但给出了推理速度（47.5 FPS）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了全方位的对比实验，涵盖了从传统 CNN 到最新实时检测器（如 YOLOv10, RT-DETR）的 9 种主流模型。
*   **消融实验**：采用了 $2^3$ 全因子消融实验，逐一验证了 MambaEFP、DFMamba 和损失函数组合的有效性。
*   **客观性**：通过跨域数据集（FLS, MD-FLS）验证了模型在不同声呐设备和环境下的泛化能力，而非仅在单一数据集上过拟合，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先**：MambaDSF 在 UATD 基准上达到 **91.5% mAP50**，优于所有对比模型。
*   **小目标优势**：在小目标子集上，相比基准模型获得了 **+2.2 pp** 的显著增益。
*   **效率平衡**：参数量（28.7 M）比大多数主流模型更轻量，虽然由于 SSM 的递归特性和多尺度融合导致 GFLOPs 略高，但仍能保持 47.5 FPS 的实时检测速度。
*   **泛化性**：在跨域测试中，其 mAP50-95 指标均位列第一，证明了全局上下文建模对抑制领域噪声的有效性。

#### 7. 优点
*   **架构创新**：成功将 Mamba 的线性复杂度优势引入声呐领域，并解决了 1D 序列化导致的局部特征丢失问题。
*   **针对性强**：SA-WIoU 和 CSC 损失函数精准解决了声呐小目标检测中“IoU 易为零”和“跨尺度不一致”的痛点。
*   **实用性高**：在保持高精度的同时实现了实时推理，适合部署于 AUV（自主水下航行器）等嵌入式平台。

#### 8. 不足与局限
*   **应用场景局限**：目前仅在前视声呐（FLS）数据集上进行了验证，尚未在侧扫声呐（SSS）或合成孔径声呐（SAS）上测试。
*   **实验环境**：实验数据主要来源于实验室或特定海域环境，实际复杂海洋环境（如极端浑浊、多路径干扰严重）下的表现仍待观察。
*   **计算开销**：虽然是线性复杂度，但多分支空洞注意力和跨尺度调制增加了实际的计算负担（GFLOPs 较高），可能影响在极低功耗设备上的表现。

（完）
