---
title: "SLIP-RS: Structured-Attribute Language-Image Pre-Training for Remote Sensing Object Detection"
title_zh: SLIP-RS：面向遥感目标检测的结构化属性语言-图像预训练
authors: "Chenxu Wang, Yuxuan Li, Yunheng Li, Xiang Li, Jingyuan Xia, Qibin Hou"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23144v1"
tags: ["query:sod"]
score: 8.5
evidence: 用于遥感目标检测的语言图像预训练，具有细粒度表示
tldr: 针对遥感目标检测中数据稀缺和细粒度表征难题，本文提出SLIP-RS框架。该框架通过结构化属性解耦范式，将开放类别空间映射到有限且具物理意义的属性空间。核心包含结构化属性对比学习和基于共形预测的可靠性引擎，并构建了包含1500万标注的RS-Attribute-15M数据集。实验证明该方法在细粒度检测和跨域泛化上达到了领先水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23144v1/fig-001.webp\", \"caption\": \"Table 2. Comparison with existing methods on DOTA-v2.0 (HBB) and DIOR-H. SLIP-RS denotes the baseline trained without attribute supervision, while SLIP-RS* refers to the model trained with our SACL strategy incorporating fine-grained attribute annotations. The base detectors of LAE-DINO and OpenRSD are DINO and RTMDet, respectively.\", \"page\": 6, \"index\": 1, \"width\": 1021, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2605.23144v1/fig-002.webp\", \"caption\": \"Table 3. Quantitative results of atomic fine-grained attribute recognition on the constructed benchmark. ‘P’ for Purpose, ‘U’ for Usage, ‘EP’ for Engine Position, ‘WC’ for Wing Configuration, ‘NE’ for Number of Engine, ‘PT’ for Propulsion Type, ‘S’ for Subcategory.\", \"page\": 7, \"index\": 2, \"width\": 998, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2605.23144v1/fig-003.webp\", \"caption\": \"Figure 3. Visualizations of SLIP-RS for attribute-guided finegrained detection. SLIP-RS not only identifies fine-grained categories but also enables recognition based on specific attributes.\", \"page\": 7, \"index\": 3, \"width\": 486, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2605.23144v1/fig-004.webp\", \"caption\": \"Figure 5. Demo examples from the Seed Attribute Classification Dataset.\", \"page\": 14, \"index\": 4, \"width\": 998, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2605.23144v1/fig-005.webp\", \"caption\": \"Figure 4. Visualization of detection results using SLIP-RS. The figure showcases the model’s ability to localize targets based on compositional attributes and identify fine-grained categories. Note that the samples in (d), (e), and (j) are generated by Nano Banana Pro.\", \"page\": 8, \"index\": 5, \"width\": 1008, \"height\": 397}, {\"url\": \"assets/figures/arxiv/2605.23144v1/fig-006.webp\", \"caption\": \"Figure 2. Overview of Structured-Attribute Contrastive Learning. (a) Positive prompts are generated via Random Drop and Shuffle to enforce permutation invariance. Hard negatives are synthesized via Attribute Replacement, creating counterfactual prompts for fine-grained discrimination. (b) & (c) Unified Learning: The model optimizes a unified contrastive objective, which aligns representations at the (b) global image level for classification and adapts to the (c) local region level (via RoI features) for detection.\", \"page\": 4, \"index\": 6, \"width\": 1009, \"height\": 305}]"
motivation: 现有的遥感图文预训练依赖于穷举类别的整体标签学习，难以应对领域内的数据稀缺和细粒度识别挑战。
method: 提出结构化属性解耦范式，利用对比学习强化属性逻辑，并通过共形可靠性引擎从噪声数据中提取高质量标注。
result: 成功构建了规模最大的遥感属性数据集RS-Attribute-15M，并在细粒度检测和跨域泛化任务中刷新了性能记录。
conclusion: 结构化属性是提升遥感目标检测性能和泛化能力的关键基础，为解决遥感领域数据瓶颈提供了新路径。
---

## 摘要
现有的遥感目标检测语言-图像预训练受限于单体标签学习（Monolithic Label Learning），该方法依赖于通过黑盒数据穷举开放集类别以获取细粒度表示，导致其依赖性与该领域固有的数据稀缺性不相容。为了突破这一瓶颈，我们提出了 SLIP-RS，建立了一种结构化属性解耦范式，将开放式类别空间映射到有限且具有物理意义的属性空间，通过显式的结构逻辑解锁细粒度判别能力。该范式由两个技术支柱实现：(1) 结构化属性对比学习，通过组合属性增强强制学习解耦的内在视觉逻辑；(2) 共形属性可靠性引擎，利用共形预测理论从噪声源中严格提取高保真监督信息，从而构建了包含超过 1500 万条属性标注的最大数据集 RS-Attribute-15M。广泛的实验表明，SLIP-RS 在细粒度检测和跨域泛化方面取得了前所未有的性能，验证了结构化属性作为遥感领域重要基础的价值。代码：https://github.com/facias914/SLIP-RS。

## Abstract
Existing language-image pre-training for remote sensing object detection is constrained by Monolithic Label Learning, which relies on exhaustively enumerating open-set categories via black-box data to acquire fine-grained representations, creating a dependency incompatible with the domain's inherent data scarcity. To transcend this bottleneck, we propose SLIP-RS, establishing a Structured-Attribute Decoupling Paradigm that maps the open-ended category space into a finite, physically meaningful attribute space, unlocking fine-grained discriminability via explicit structural logic. This paradigm is realized via two technical pillars: (1) Structured-Attribute Contrastive Learning, which enforces the learning of decoupled intrinsic visual logic via combinatorial attribute augmentation; and (2) Conformal Attribute Reliability Engine, which leverages conformal prediction theory to rigorously distill high-fidelity supervision from noisy sources, yielding RS-Attribute-15M, the largest dataset with over 15 million attribute annotations. Extensive experiments demonstrate that SLIP-RS establishes unprecedented performance in fine-grained detection and cross-domain generalization, validating structured attributes as a vital foundation for remote sensing. Code: https://github.com/facias914/SLIP-RS.

---

## 论文详细总结（自动生成）

以下是对论文《SLIP-RS: Structured-Attribute Language-Image Pre-Training for Remote Sensing Object Detection》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的遥感（RS）目标检测预训练主要遵循“单体标签学习”（Monolithic Label Learning）范式，即将每个类别视为一个不可分割的语义黑盒（如“波音747”）。这种方法依赖于海量图文对来穷举开放世界类别，但遥感领域面临严重的数据稀缺，且模型难以捕捉区分相似类别的细粒度物理特征（如引擎数量、机翼形状）。
*   **核心问题**：如何在数据稀缺的遥感领域，突破语义绑定的限制，建立具备极强细粒度判别能力和跨域泛化能力的预训练模型？

### 2. 论文提出的方法论
SLIP-RS 建立了**结构化属性解耦范式**，将开放的类别空间映射到有限的、具有物理意义的属性空间。
*   **核心思想**：将物体分解为一组正交且互斥的语义原语（Semantic Primitives）。例如，不再仅仅识别“飞机”，而是识别“4个引擎 + 后掠翼 + 喷气式”的组合。
*   **关键技术一：结构化属性对比学习 (SACL)**
    *   **正样本构建**：通过对属性集合进行随机打乱（Shuffle）和随机丢弃（Drop），强制模型学习排列无关的集合表示，模拟遮挡等复杂场景。
    *   **硬负样本挖掘**：利用属性的互斥性进行“属性替换”（如将“双发”替换为“四发”），生成反事实（Counterfactual）文本描述，迫使模型关注细微的视觉差异。
*   **关键技术二：共形属性可靠性引擎 (CARE)**
    *   **流程**：微调（建立教师模型） -> 校准（利用共形预测理论） -> 扩展（大规模伪标签生成）。
    *   **统计保证**：CARE 放弃了启发式的固定阈值，引入**共形预测（Conformal Prediction）**理论，通过严格的统计边界控制错误发现率（FDR），从噪声数据中提取高保真标注，从而构建了包含 1500 万个实例标注的 **RS-Attribute-15M** 数据集。

### 3. 实验设计
*   **数据集与场景**：
    *   **通用检测**：DOTA-v2.0, DIOR-H。
    *   **细粒度识别**：基于 Fair1M, MAR20, HRSC2016 构建的属性识别 Benchmark。
    *   **下游任务**：Xview（大规模细粒度分类）、DOTA-v2.0 OBB（旋转目标检测）。
*   **对比方法**：包括传统的 Faster R-CNN、DINO、RTMDet，以及针对遥感优化的 LAE-DINO、OpenRSD、ViTP、RemoteCLIP 等。
*   **评估指标**：mAP, AP50 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 块 NVIDIA A40 GPU**。
*   **训练细节**：采用 AdamW 优化器，初始学习率 $1 \times 10^{-5}$，权重衰减 $5 \times 10^{-2}$。
*   **时长**：总计训练 12 个 epoch，在第 8 和 11 个 epoch 进行学习率衰减。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括零样本（Zero-shot）迁移、有监督微调、原子属性验证、组合属性验证以及消融实验。
*   **充分性与客观性**：
    *   实验涵盖了从 ConvNeXT-T 到 ConvNeXT-L 的不同参数规模。
    *   对比了当前最先进的遥感大模型（如参数量达 2.4B 的 BFM），证明了 SLIP-RS 在更小参数量下仍具竞争力。
    *   使用了 NanoBanana Pro 生成训练集中未见的“反事实”样本进行可视化验证，增强了结论的说服力。

### 6. 主要结论与发现
*   **性能领先**：SLIP-RS 在 DOTA-v2.0 和 DIOR-H 上显著超过了 OpenRSD 等现有方法。
*   **细粒度优势**：在属性识别任务中，SLIP-RS 的表现远超传统方法（如在飞机用途识别上，mAP 从 9.03% 提升至 72.04%）。
*   **组合泛化**：模型展现了通过属性组合识别新类别的能力，证明了属性解耦能有效缓解“语义绑定”问题。
*   **预训练价值**：作为骨干网络，SLIP-RS 在旋转框检测和大规模细粒度分类任务中均表现出极强的迁移能力。

### 7. 优点（亮点）
*   **范式创新**：从“标签匹配”转向“属性解耦”，更符合人类对物理世界的认知逻辑。
*   **数据贡献**：发布了 RS-Attribute-15M，填补了遥感领域大规模属性标注数据集的空白。
*   **理论严谨**：引入共形预测（CARE 引擎）来处理伪标签噪声，比单纯设置置信度阈值更具科学性。
*   **可视化深入**：通过激活图证明了模型确实关注到了引擎、机翼等特定物理区域。

### 8. 不足与局限
*   **类别覆盖不均**：目前的结构化属性主要集中在飞机、舰船、车辆三大类，对于桥梁、操场等结构简单的类别，属性定义尚不够丰富。
*   **计算开销**：虽然推理时 text encoder 可以冻结，但训练阶段构建实例特异性的正负样本对会增加一定的计算和存储负担。
*   **应用限制**：对于极低分辨率的遥感图像，属性（如引擎位置）可能因像素不足而无法辨识，导致该方法在低质数据上的增益受限。

（完）
