---
title: Representative Attention For Vision Transformers
title_zh: Vision Transformer 的代表性注意力机制
authors: "Yuntong Li, Hainuo Wang, Hengxing Liu, Mingjia Li, Xiaojie Guo"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14913v1"
tags: ["query:sod"]
score: 6.5
evidence: 视觉Transformer中表示空间的动态令牌压缩
tldr: 本研究针对视觉Transformer（ViT）中自注意力机制计算量随标记数量平方增长的问题，提出了代表性注意力（RPAttention）。该方法摒弃了传统的基于固定空间布局的标记压缩方式，转而采用“聚合-交互-分发”范式，在表示空间内动态生成代表性标记。这种线性全局注意力机制不仅将计算复杂度降至线性，还实现了基于语义内容的全局信息交换，在图像分类、目标检测和语义分割等任务中表现出色。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14913v1/fig-001.webp\", \"caption\": \"Figure 3: Analysis of representative token construction. Left: translation robustness under shifted inputs. Middle: ImageNet-1K accuracy-FLOPs trade-off across efficient attention variants. Right: query-response visualization.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2605.14913v1/fig-002.webp\", \"caption\": \"Figure 1: Motivation of Representative Attention. (a) An input image with three query locations Q1, Q2, and Q3. (b) Spatial token compression constructs intermediate tokens from fixed coordinate layouts, which may not align with the semantic organization of visual features. (c) Representative Attention gathers spatial tokens into learned representative tokens according to representation similarity. (d) Spatial attention responses are affected by coordinate-based grouping. (e) Representative Attention produces representation-driven responses that better align with object-level visual structures.\", \"page\": 2, \"index\": 2, \"width\": 826, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2605.14913v1/fig-003.webp\", \"caption\": \"Table 1: ImageNet-1K classification comparison. Baseline results follow the corresponding backbone papers, FLatten Transformer [47], Agent Attention [21], and PolaFormer [12].\", \"page\": 7, \"index\": 3, \"width\": 826, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2605.14913v1/fig-004.webp\", \"caption\": \"Figure A.2: Efficiency comparison under different input resolutions on an NVIDIA RTX 4090D GPU with batch size 32 and AMP/FP16 inference. Left: throughput in images per second. Middle: relative speedup of RPAttention-PVT-Tiny over PVT-Tiny and Agent-PVT-Tiny. Right: CUDA inference latency in milliseconds.\", \"page\": 14, \"index\": 4, \"width\": 826, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2605.14913v1/fig-005.webp\", \"caption\": \"Figure A.1: Additional query-response visualizations. For selected query locations, RPAttention produces response maps that align with semantically related image regions, illustrating how representatives mediate global information exchange beyond fixed spatial layouts.\", \"page\": 13, \"index\": 5, \"width\": 664, \"height\": 970}, {\"url\": \"assets/figures/arxiv/2605.14913v1/fig-006.webp\", \"caption\": \"Table 2: COCO object detection (Mask R-CNN) and ADE20K semantic segmentation (SemanticFPN). Baseline results follow Agent Attention [21]. Detection FLOPs are computed with input resolution 1333× 800. Segmentation FLOPs are computed with input resolution 512× 2048.\", \"page\": 8, \"index\": 6, \"width\": 668, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2605.14913v1/fig-007.webp\", \"caption\": \"Figure 2: Overview of Representative Attention. Spatial tokens are gathered into a compact set of representatives according to representation similarity (Gather). These tokens then model global dependencies through lightweight self-attention in the compact token space (Interact). Finally, the refined representations are distributed back to all spatial tokens through query-driven cross-attention (Distribute). A parallel depth-wise convolution branch preserves local structural details.\", \"page\": 5, \"index\": 7, \"width\": 826, \"height\": 496}]"
motivation: 现有的线性注意力方法多依赖预定义的空间布局进行标记压缩，导致信息交换受限于图像坐标而非视觉内容的语义组织。
method: 提出RPAttention，通过相似度路由将空间标记软聚合为代表性标记，在潜在空间进行全局交互后，再通过查询驱动的交叉注意力将信息分发回原标记。
result: 该方法成功将标记交互复杂度从平方级降低到线性级，并在多种视觉任务和骨干网络上验证了其有效性。
conclusion: RPAttention通过表示驱动的压缩机制，在保持全局感受野的同时，实现了与输入内容结构自适应对齐的高效通信。
---

## 摘要
线性注意力已成为将 Vision Transformer 扩展到密集自注意力二次代价之外的一个有前景的方向。一种流行的策略是将空间 token 压缩为一组紧凑的中间代理，以调解全局信息交换。然而，现有方法通常从预定义的空间布局中导出这些代理 token，导致 token 压缩仍然锚定在图像坐标上，而非视觉内容的语义组织。为了克服这一局限性，我们提出了代表性注意力（Representative Attention，RPAttention），这是一种直接在表示空间中执行 token 压缩的线性全局注意力机制。它不再从固定的空间分区构建中间 token，而是遵循轻量级的“聚合-交互-分发”（Gather-Interact-Distribute）范式，动态形成一组学习到的紧凑代表性 token，使语义相关的区域无论空间距离如何都能进行通信。空间 token 首先通过基于相似性的竞争性路由软聚合到代表性 token 中。随后，代表性 token 在紧凑的潜空间内进行全局交互，然后通过查询驱动的交叉注意力将细化后的信息广播回所有空间 token。通过用表示驱动的压缩取代坐标驱动的聚合，RPAttention 在保持全局感受野的同时，自适应地使 token 通信与每个输入的内容结构对齐。RPAttention 将主要的 token 交互复杂度从相对于空间 token 数量的二次方降低到线性缩放，同时保持了极具表现力的全局上下文建模。在图像分类、目标检测和语义分割等多种 Vision Transformer 骨干网络上进行的广泛实验证明了我们设计的有效性。

## Abstract
Linear attention has emerged as a promising direction for scaling Vision Transformers beyond the quadratic cost of dense self-attention. A prevalent strategy is to compress spatial tokens into a compact set of intermediate proxies that mediate global information exchange. However, existing methods typically derive these proxy tokens from predefined spatial layouts, causing token compression to remain anchored to image coordinates rather than the semantic organization of visual content. To overcome this limitation, we propose Representative Attention (RPAttention), a linear global attention mechanism that performs token compression directly in representation space. Instead of constructing intermediate tokens from fixed spatial partitions, it dynamically forms a compact set of learned representative tokens to enable semantically related regions to communicate regardless of their spatial distance, by following a lightweight Gather-Interact-Distribute paradigm. Spatial tokens are first softly gathered into representative tokens through competitive similarity-based routing. The representatives then perform global interaction within a compact latent space, before broadcasting the refined information back to all spatial tokens via query-driven cross-attention. Via replacing coordinate-driven aggregation with representation-driven compression, RPAttention preserves global receptive fields while adaptively aligning token communication with the content structure of each input.RPAttention reduces the dominant token interaction complexity from quadratic to linear scaling with respect to the number of spatial tokens, while maintaining expressive global context modeling. Extensive experiments across diverse vision transformer backbones on image classification, object detection, and semantic segmentation demonstrate the effectiveness of our design.